using UnityEngine;

public class UIGameModeSelect : MonoBehaviour
{
    //デバック用
    private void Awake()
    {
        GameBase.scene = "GameModeSelect";
    }

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
